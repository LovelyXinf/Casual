<div class="content-page">
    <div class="ui-title">{% helper lang.l('header_main', 'mailbox') %}</div>
    <div>
        {% helper lang.l('field_usr_inf_description', 'mailbox') %}
    </div>
    {% for link, name in pages %}
        <div><a href="{{ link }}">{{ name }}</a></div>
    {% else %}
        {% if is_not_archive %}
            <input type="checkbox" name="ui_mailbox" data-module="mailbox" value="1" class="ui_checkbox" data-action="ui_change" checked>
        {% else %}
            <div><i>{% helper lang.l('list_empty', 'mailbox') %}</i></div>
        {% endif %}
    {% endfor %}
</div>